def init_mat(x, y): return [[0 for _ in range(x)] for _ in range(y)]
#def init_mat(x, y): return [[0] * x] * y # :)
# TODO: UPyLab Marche plus, encore un peu et tout le systeme irat crash
"""
def init_mat(x,y):
    m = []
    for i in range(0, y):
        r = []
        for j in range(0, x): r += [0]
        m += [r]
    return m
"""